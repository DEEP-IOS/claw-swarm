/**
 * Store 重导出 / Store Re-export
 *
 * 保持向后兼容 — 所有 `import useStore from './store.js'` 继续工作。
 * 实际 Store 定义在 `./store/index.js`。
 *
 * @module console/store
 * @author DEEP-IOS
 */
export { default } from './store/index.js';
